# Conexao com o Postgres do Supabase self-hosted (porta = POSTGRES_HOST_PORT no docker, default 15432).
# User: supabase_admin (a imagem supabase/postgres nao cria o role "postgres").
DATABASE_URL=postgresql://supabase_admin:rachao-dev-password-change-me@127.0.0.1:15432/postgres?schema=public
